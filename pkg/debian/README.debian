Suggested dependencies missing in Debian:

- monitor service using IRC: python3-irc
- monitor service using web sockets: python3-autobahn
- expose service: python3-autobahn

These are not packaged for Debian yet but you can install them this way:

# apt-get install python3-pip
# pip3 install autobahn
# pip3 install irc
